-- ============================================================
-- Roxlogy — race_events.api_city
--
-- 대회 상세 페이지가 Result API 의 이벤트(디비전×요일 행)를 찾을 때
-- 도시로 매칭해야 하는데, 우리 city 는 한국어 표기다. 동기화 스크립트가
-- API 원문 도시명(영문, 연도 프리픽스 제거)을 함께 저장해 둔다.
-- 큐레이션 행은 null — 웹 쪽 역매핑 폴백을 쓴다.
-- ============================================================

alter table public.race_events
  add column if not exists api_city text;
